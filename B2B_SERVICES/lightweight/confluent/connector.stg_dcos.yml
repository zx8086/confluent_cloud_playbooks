heartbeat.monitors:

- type: http
  name: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_ARTICLES
  id: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_ARTICLES
  enabled: true
  urls: "http://kafka-connect.beta53.kafka.marathon.dcos-eu.pvhcorp.com:8083/connectors/C_SOURCE_PIM_MDM_ARTICLES/status"
  max_redirects: '3'
  schedule: '@every 1m'
  timeout: 16s
  tags: ["digital selling", "staging", "confluent"]
  alert.status.enabled: true
  check.request:
    method: GET
  check.response:
    status: [200]
    body:
      negative:
        - PROVISIONING
        - UNASSIGNED
        - STOPPED
        - PAUSED
        - FAILED
  response:
    include_body: 'always'

- type: http
  name: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_PRODUCTS
  id: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_PRODUCTS
  enabled: true
  urls: "http://kafka-connect.beta53.kafka.marathon.dcos-eu.pvhcorp.com:8083/connectors/C_SOURCE_PIM_MDM_PRODUCTS/status"
  max_redirects: '3'
  schedule: '@every 1m'
  timeout: 16s
  tags: ["digital selling", "staging", "confluent"]
  alert.status.enabled: true
  check.request:
    method: GET
  check.response:
    status: [200]
    body:
      negative:
        - PROVISIONING
        - UNASSIGNED
        - STOPPED
        - PAUSED
        - FAILED
  response:
    include_body: 'always'

- type: http
  name: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_VARIANTS
  id: DS - Kafka Connector - stg_dcos | C_SOURCE_PIM_MDM_VARIANTS
  enabled: true
  urls: "http://kafka-connect.beta53.kafka.marathon.dcos-eu.pvhcorp.com:8083/connectors/C_SOURCE_PIM_MDM_VARIANTS/status"
  max_redirects: '3'
  schedule: '@every 1m'
  timeout: 16s
  tags: ["digital selling", "staging", "confluent"]
  alert.status.enabled: true
  check.request:
    method: GET
  check.response:
    status: [200]
    body:
      negative:
        - PROVISIONING
        - UNASSIGNED
        - STOPPED
        - PAUSED
        - FAILED
  response:
    include_body: 'always'

